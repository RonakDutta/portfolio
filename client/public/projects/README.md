# Project screenshots

Filenames referenced by `image` in `src/data/content.js`:

- `ats-workplace.png`
- `business40.png`
- `safarsaathi.png`

16:10, at least 1600×1000. Anything missing renders a labelled placeholder at
the correct ratio, so the layout does not move when you add the file.
